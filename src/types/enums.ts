export enum Player {
    X = "X",
    O = "O",
}

export enum GameStatus {
    INITIALIZED = "initialized",
    IN_PROGRESS = "in_progress",
    WON = "won",
    DRAW = "draw",
    ABANDONED = "abandoned",
}